# Admin Preview QA

This document records the non-production QA checkpoint for the private B2B inquiry dashboard.

- Preview authentication must be verified before any production promotion.
- Production must remain on the currently active version until QA is complete.
- Never record or commit `ADMIN_TOKEN` values in the repository.
