# Agro-Zia Inquiry Preview Refresh

Preview-only build trigger for Phase 1 QA.

The current QA branch already contains the hardened `AGROZIA_DB` inquiry core and canonical multilingual inquiry replacement. This commit exists only to force a fresh Cloudflare branch preview from the current QA head so the stale commit-preview URL is not reused.

`main` is intentionally unchanged.